// Minimal ID/type aliases shared across apps
// These mirror definitions in the mobile app but avoid importing from it.

type UserID = string;
type DeviceId = string;
type FeedbackId = string;
type BugId = string;
type ReasonForLeavingId = string;
type NicknameKey = string;

export type {
  UserID,
  DeviceId,
  FeedbackId,
  BugId,
  ReasonForLeavingId,
  NicknameKey,
};
