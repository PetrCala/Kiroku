/** Native builds already are the app, so there is nothing to download. */
function InviteAppDownloadButtons() {
  return null;
}

InviteAppDownloadButtons.displayName = 'InviteAppDownloadButtons';
export default InviteAppDownloadButtons;
